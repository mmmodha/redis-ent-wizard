(app) {
	# Next.js owns /credentials and /instances/* ; Fastify is only under /api.
	@api path /api*
	handle @api {
		uri strip_prefix /api
		reverse_proxy api:4000 {
			flush_interval -1
		}
	}

	handle {
		reverse_proxy web:3000
	}
}

:8080 {
	import app
}
