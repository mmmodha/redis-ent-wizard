(app) {
	@api path /health /auth/* /credentials* /instances* /gcp* /preflight /folders /owners /admin* /releases* /artifacts*
	handle @api {
		reverse_proxy api:4000 {
			flush_interval -1
		}
	}

	handle {
		reverse_proxy web:3000
	}
}

:8080 {
	import app
}
